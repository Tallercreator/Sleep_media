<%= form_with(model: product_card) do |form| %>
  <% if product_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_card.errors.count, "error") %> prohibited this product_card from being saved:</h2>

      <ul>
        <% product_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div class="field">
  <%= form.label :image, "Изображение" %><br>
  <%= form.file_field :image %>
</div>

<% if @product_card.image.attached? %>
  <p>Текущее изображение:</p>
  <%= image_tag @product_card.image, width: 200 %>
<% end %>


  <div>
    <%= form.submit %>
  </div>
<% end %>
