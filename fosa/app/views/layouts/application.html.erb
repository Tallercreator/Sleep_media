<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Fosa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>

    <!-- Здесь подключаем именно application.css,  
         в который Tailwind «вреагировал» свои правила -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-50 text-gray-800">

    <%= render "layouts/header" %>

    <!-- Первый экран (hero) только на главной -->
    <% if current_page?(root_path) %>
      <%= render "home/hero" %>
    <% end %>

    <!-- Контент страницы -->
    <main class="container mx-auto px-6 lg:px-0 py-12">
      <%= yield %>
    </main>

    <%= render "layouts/footer" %>
  </body>
</html>
